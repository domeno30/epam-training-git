- hosts: all
  tasks:
  - name: yum update & upgrade
    yum:
      name: '*'
      state: latest
  - name: Remote RPM
    yum: name=https://dev.mysql.com/get/mysql80-community-release-el7-3.noarch.rpm

  - name: Install nginx
    yum:
      name: nginx
      state: present
